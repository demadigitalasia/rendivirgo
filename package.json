{
  "name": "rendi-virgo-storefront",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "tsc --noEmit",
    "dev:api": "npm --prefix api run dev",
    "dev:all": "npm run dev:api & npm run dev",
    "build:api": "npm --prefix api run build",
    "db:migrate": "npm --prefix api run prisma:migrate",
    "db:seed": "npm --prefix api run db:seed",
    "db:studio": "npm --prefix api run prisma:studio"
  },
  "dependencies": {
    "next": "16.3.3",
    "react": "19.2.0",
    "react-dom": "19.2.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "typescript": "7.0.2"
  }
}
